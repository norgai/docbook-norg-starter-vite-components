settings: "CMS": "Sanity", "THEME": "{THEME}", "COMPONENT": "{COMPONENT}"

File 1: Edit File:  "src/components/theme/{THEME}/components/{COMPONENT}/figmaToCode*.tsx"
    Description: Contains all the variants
    Instructions:
        - Verify that each variant contains a wrapper div with the id "{COMPONENT}"
        - If missing ensure this is present, don't touch anything else
